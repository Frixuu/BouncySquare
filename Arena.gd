extends Node

export var width: int = 720
export var height: int = 240

export var x_max: float = float(width / 2)
export var x_min: float = -x_max

export var y_max: float = float((height / 2) - (32 / 2))
export var y_min: float = -y_max
